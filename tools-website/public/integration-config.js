// Public same-origin bridge only. Never put gateway addresses or credentials here.
window.L3V_API = { enabled: false };
// Set both profile URLs during deployment. Empty values keep unfinished payment links disabled.
window.L3V_SUPPORT = { kofi: '', github: '' };
