import {randomBytes,createHash} from 'node:crypto';
import {mkdir,writeFile,readFile} from 'node:fs/promises';
import path from 'node:path';
import QRCode from 'qrcode';
const count=Number(process.argv[2]||100),out=path.resolve(process.argv[3]||'private-invitations');
if(!Number.isInteger(count)||count<1||count>1000)throw Error('Count must be 1-1000');
await mkdir(out,{recursive:false});const asset=name=>'data:image/png;base64,';const encoded={};for(const name of ['magic-name.png','magic-initials.png','magic-signature.png'])encoded[name]=asset(name)+(await readFile(path.resolve('public/assets/identity',name))).toString('base64');const records=[];
for(let i=1;i<=count;i++){const token=randomBytes(32).toString('base64url'),id=String(i).padStart(3,'0'),payload=JSON.stringify({type:'l3v-access',version:1,token});const qr=await QRCode.toString(payload,{type:'svg',errorCorrectionLevel:'H',margin:1,width:420,color:{dark:'#171712',light:'#faf9f3'}}),inner=qr.replace(/^<svg[^>]*>|<\/svg>$/g,'');const certificate=`<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="760" viewBox="0 0 1200 760"><rect width="1200" height="760" fill="#faf9f3"/><rect x="28" y="28" width="1144" height="704" rx="24" fill="none" stroke="#726b39" stroke-width="3"/><text x="90" y="130" font-family="Arial,sans-serif" font-size="24" letter-spacing="5" fill="#726b39">L3V AI TOOLS</text><text x="90" y="210" font-family="Arial,sans-serif" font-size="52" fill="#171712">Private invitation</text><text x="90" y="268" font-family="Arial,sans-serif" font-size="22" fill="#57574f">This image is your key to all work created with this invitation.</text><text x="90" y="308" font-family="Arial,sans-serif" font-size="22" fill="#57574f">Keep it private. Upload it at tools.l3v.ai to return.</text><g transform="translate(90 355)"><rect width="170" height="170" rx="16" fill="#eeede5"/><image href="${encoded['magic-name.png']}" x="15" y="15" width="140" height="120" preserveAspectRatio="xMidYMid meet"/><text x="85" y="155" text-anchor="middle" font-family="Arial" font-size="14">NAME LOGO</text></g><g transform="translate(280 355)"><rect width="170" height="170" rx="16" fill="#eeede5"/><image href="${encoded['magic-initials.png']}" x="15" y="15" width="140" height="120" preserveAspectRatio="xMidYMid meet"/><text x="85" y="155" text-anchor="middle" font-family="Arial" font-size="14">INITIALS</text></g><g transform="translate(470 355)"><rect width="170" height="170" rx="16" fill="#eeede5"/><image href="${encoded['magic-signature.png']}" x="15" y="15" width="140" height="120" preserveAspectRatio="xMidYMid meet"/><text x="85" y="155" text-anchor="middle" font-family="Arial" font-size="14">SIGNATURE</text></g><g transform="translate(700 140)">${inner}</g><text x="90" y="615" font-family="Arial,sans-serif" font-size="20" fill="#171712">Invitation ${id}</text><text x="90" y="655" font-family="monospace" font-size="15" fill="#57574f">${token}</text></svg>`;await writeFile(path.join(out,`l3v-invitation-${id}.svg`),certificate);records.push({id,token,hash:createHash('sha256').update(token).digest('hex')});}
await writeFile(path.join(out,'PRIVATE-invitations.json'),JSON.stringify(records,null,2));await writeFile(path.join(out,'invitation-hashes.json'),JSON.stringify(records.map(({id,hash})=>({id,hash})),null,2));console.log(`Generated ${count} private invitation certificates in ${out}`);
