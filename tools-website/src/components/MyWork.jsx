import {useEffect,useState} from 'react';import {accessFetch,requestReceipt} from '../lib/master-access.mjs';import {importAccountWork} from '../lib/video-receipt.mjs';import {remember} from '../lib/name-logo-request.mjs';
export default function MyWork({hidden}){const [groups,setGroups]=useState([]),[message,setMessage]=useState('');useEffect(()=>{if(hidden)return;accessFetch('/api/account/work').then(r=>{if(!r.ok)throw Error();return r.json()}).then(({work=[]})=>{const map=new Map();for(const row of work){const list=map.get(row.request_id)||[];list.push(row);map.set(row.request_id,list)}setGroups([...map.values()])}).catch(()=>setMessage('Your work index is temporarily unavailable.'))},[hidden]);async function open(rows){const root=rows[0];if(root.scope==='video'){await importAccountWork(rows);location.hash='video';location.reload();return}const portfolio=rows.find(r=>r.resource_kind==='name-logo-portfolio');if(!portfolio)return;remember({access:{requestId:root.request_id,receipt:await requestReceipt(root.request_id)},requestKey:root.request_id,id:portfolio.resource_id,first:portfolio.first||'',last:portfolio.last||''});location.hash='logo';location.reload()}return <section className="my-work" hidden={hidden}><p className="eyebrow">PRIVATE LIBRARY</p><h1>My work</h1><p>Everything still retained for this invitation.</p>{message&&<p role="status">{message}</p>}<div className="work-list">{groups.map(rows=><article key={rows[0].request_id}><div><strong>{rows[0].scope==='video'?'Video project':`Identity designs${rows.find(r=>r.first)?.first?' for '+rows.find(r=>r.first).first+' '+rows.find(r=>r.first).last:''}`}</strong><small>{new Date(rows[0].created).toLocaleString()}</small></div><button onClick={()=>open(rows)}>Open</button></article>)}</div></section>}
