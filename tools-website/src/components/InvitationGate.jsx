import {useEffect,useState} from 'react';import {accessToken,saveAccessToken,clearAccessToken,tokenFromImage,receiptImage} from '../lib/master-access.mjs';
async function validate(credential){const response=await fetch('/api/invitation/validate',{headers:{'X-L3V-Invitation':credential}});if(!response.ok)throw Error('This invitation is not valid.');return response.json()}
export default function InvitationGate({children}){const [ready,setReady]=useState(false),[busy,setBusy]=useState(true),[message,setMessage]=useState('Checking invitation…'),[phrase,setPhrase]=useState('');async function enter(credential){setBusy(true);try{const result=await validate(credential);saveAccessToken(credential,result.accountId);setReady(true);setMessage('')}catch(e){clearAccessToken();setReady(false);setMessage(e.message)}finally{setBusy(false)}}useEffect(()=>{const credential=accessToken();if(credential)enter(credential);else{setBusy(false);setMessage('Upload your certificate or enter its three-word secret.')}},[]);async function upload(file){if(!file)return;setMessage('Reading certificate…');try{await enter(await tokenFromImage(file))}catch(e){setBusy(false);setMessage(e.message)}}async function download(){const a=document.createElement('a');a.href=await receiptImage();a.download='l3v-private-access-receipt.png';a.click()}if(!ready)return <main className="invitation-gate"><p className="eyebrow">INVITATION ONLY</p><h1>Your invitation is your key.</h1><p>Upload your private certificate or type its three-word secret.</p><label className="certificate-upload"><input type="file" accept="image/png,image/jpeg,image/webp,image/svg+xml" onChange={e=>upload(e.target.files[0])}/><strong>Upload invitation certificate</strong><span>Choose the private image you received</span></label><div className="invitation-phrase"><label htmlFor="invitation-phrase">Three-word secret</label><div><input id="invitation-phrase" value={phrase} onChange={e=>setPhrase(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&phrase.trim())enter(phrase)}} placeholder="Purplellama bovikazume tapenorixo"/><button disabled={busy||!phrase.trim()} onClick={()=>enter(phrase)}>Enter</button></div></div><p role="status">{busy?'Checking…':message}</p></main>;return <><div className="access-bar"><span>Private invitation active</span><button onClick={download}>Download access receipt</button><button onClick={()=>{clearAccessToken();location.reload()}}>Lock</button></div>{children}</>}
