import type { Metadata } from 'next';
import './globals.css';
export const metadata:Metadata={title:'AI Video Model Field Guide — September 2026',description:'Strengths, limitations, best uses and real demo imagery for the AI video model landscape.'};
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
